// import React from 'react'
// import Country from './Country'

// export default function Countries({ userId , data }) {
// 	let sortableCountries = data.slice().sort((a, b) => a.order - b.order)
// 	return (
// 		<div>
// 			{sortableCountries.map((country, index) => (
// 				<Country key={country.index} country={country} userId={userId} />
// 			))}
// 		</div>
// 	)
// }
